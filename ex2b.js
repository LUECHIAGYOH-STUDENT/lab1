// Exercise 2(b): Extends 2(a) - the arrow function now takes an argument
// (myVal) and outputs the passed value to the console.

let message = (myVal) => {

    console.log(myVal);
}

message("Hello World!") // Calls the function, passing a string argument
