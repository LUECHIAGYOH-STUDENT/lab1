// Simple test file: arrow function that adds two numbers together (similar
// in style to Exercise 2c), used to sanity-check the syntax.

let x = (i, j) => {
    return (i + j);

}

console.log(x(3, 2)); // Logs the result of adding 3 and 2 (5)
